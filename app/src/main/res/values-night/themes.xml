<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Eventosapp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/azul</item>
        <item name="android:navigationBarColor">@color/oscuro</item>
        <item name="android:windowBackground">@color/oscuro</item>
        <item name="android:statusBarColor">@color/oscuro</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>